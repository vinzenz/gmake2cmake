<task id="TASK-0037">
<summary>Extract magic numbers to named constants in unknowns module.</summary>
<scope>
- Centralize truncate length, ellipsis, and ID format constants in `gmake2cmake/ir/unknowns.py`.
- Update helper functions to use named constants instead of literals.</scope>
<developer>
- Introduce module-level constants (e.g., `DEFAULT_TRUNCATE_LENGTH`, `ELLIPSIS`, `UC_ID_FORMAT`) and refactor `_truncate`/`_format_uc_id` accordingly.
- Ensure behavior unchanged (same truncation semantics).</developer>
<qe>
- Extend `tests/test_unknowns.py` to verify truncation length and ID formatting respect the new constants.</qe>
<reviewer>
- Check for deterministic output and no behavioral regression from refactor.</reviewer>
<tests>
- Commands: `ruff check gmake2cmake/ir/unknowns.py && pytest -q tests/test_unknowns.py`.</tests>
</task>
