<task id="TASK-0034">
<summary>Resolve empty __all__ declaration in package init.</summary>
<scope>
- Decide whether `gmake2cmake/__init__.py` should expose a public API or drop the empty `__all__` entirely.
- Align exported symbols with intended public surface (fs adapters, diagnostics, config, CLI entry) if keeping `__all__`.
- Keep module metadata/tests updated after the change.</scope>
<developer>
- Update `__all__` to a meaningful list or remove it; ensure module docstring comments match final intent.
- Add/adjust imports only if the public surface is being exported intentionally.
- Keep ruff/import sorting clean.</developer>
<qe>
- Add/adjust tests asserting `from gmake2cmake import *` yields expected names (or confirm absence when `__all__` removed).
- Run lint to ensure no unused exports/violations.</qe>
<reviewer>
- Confirm package init matches architecture expectations and avoids exposing accidental internals.</reviewer>
<tests>
- Commands: `ruff check . && pytest -q tests/test_unknowns.py tests/test_cli.py`.</tests>
</task>
