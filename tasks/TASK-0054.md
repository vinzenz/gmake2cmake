<task id="TASK-0054">
<summary>Add configuration schema validation.</summary>
<scope>
- Define a JSON Schema (or equivalent) for the YAML config and validate loaded config files against it.
- Provide CLI support for config validation and a template/example config file.
- Add docs generation helper from the schema.</scope>
<developer>
- Create `config_schema.py` and integrate schema validation into `config.load_and_merge`/a helper; emit diagnostics for schema violations.
- Add optional CLI flag to validate config and exit; supply a sample `config_template.yaml` and doc generator if feasible.</developer>
<qe>
- Add tests covering schema validation success/failure cases, diagnostic codes, and CLI `--validate-config` behavior.</qe>
<reviewer>
- Ensure schema defaults align with existing ConfigModel behavior and validation errors are user-friendly.</reviewer>
<tests>
- Commands: `ruff check . && pytest -q tests/test_config_manager.py tests/test_cli.py`.</tests>
</task>
