<task id="TASK-0008">
<summary>Tooling/Docs: CI lint/test wiring, README/CLI entrypoint, packaging polish.</summary>
<scope>
- Add CI config to run ruff and pytest; add console_script entrypoint for CLI; expand README with usage and architecture pointers.
- Ensure packaging metadata aligns with namespace/entrypoint.</scope>
<developer>
- Introduce CI workflow (e.g., GitHub Actions) running `ruff check .` and `pytest`; update pyproject with console_scripts for gmake2cmake.
- Update README with install/run instructions, flags, packaging option; ensure version/metadata sensible.</developer>
<qe>
- Verify CI runs locally or via workflow lint; check entrypoint works (`gmake2cmake --help`).
- Ensure README instructions accurate and tested; optional badge placeholders OK.</qe>
<reviewer>
- Check workflow robustness (cache, python version); packaging metadata correctness; README concise and aligned with docs.</reviewer>
<tests>
- Commands: `ruff check . && pytest -q`.
- Manual: `pip install -e . && gmake2cmake --help`.</tests>
</task>
