<task id="TASK-0052">
<summary>Implement richer CLI exit code strategy.</summary>
<scope>
- Define an exit code enum covering common failure categories (usage/config/parse/build/emit/io/unknown-construct thresholds).
- Update CLI to return category-specific exit codes and document them.</scope>
<developer>
- Create an exit code module/helper to derive codes from diagnostics and unknown counts; integrate into `cli.run`.
- Add README/help text summarizing exit codes; ensure default remains backward compatible when no category matches.</developer>
<qe>
- Add tests asserting specific scenarios produce expected exit codes (bad args -> USAGE, config missing -> CONFIG, parse errors -> PARSE, unknown threshold -> UNKNOWN_CONSTRUCT).</qe>
<reviewer>
- Check exit code mapping is deterministic and doesn’t alter diagnostic rendering.</reviewer>
<tests>
- Commands: `ruff check . && pytest -q tests/test_cli.py tests/test_config_manager.py`.</tests>
</task>
