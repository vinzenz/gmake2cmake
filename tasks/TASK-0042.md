<task id="TASK-0042">
<summary>Document and harden FileSystemAdapter error handling.</summary>
<scope>
- Clarify expected exceptions/behaviors for fs adapter methods and add optional safe helpers.
- Align `LocalFS` with documented error handling for read/write operations.</scope>
<developer>
- Add docstrings specifying raised exceptions for adapter methods; consider adding helpers like `safe_read_text`/`can_write`.
- Update `LocalFS` to wrap IO errors with informative messages without masking specific exceptions.</developer>
<qe>
- Add tests covering read/write permission errors, missing files, and safe helper fallbacks in `tests/test_fs.py` (or new module).</qe>
<reviewer>
- Ensure new helpers do not alter adapter contract unexpectedly and diagnostics remain consistent.</reviewer>
<tests>
- Commands: `ruff check gmake2cmake/fs.py && pytest -q tests`.</tests>
</task>
