<task id="TASK-0044">
<summary>Add input validation for path normalization.</summary>
<scope>
- Harden `_normalize_ignore_paths` and other path handling utilities against invalid/empty/traversal inputs.
- Validate CLI path arguments and make discovery inputs where applicable.</scope>
<developer>
- Add checks for empty strings, traversal (`..`), and None entries when normalizing ignore patterns; raise or diagnose appropriately.
- Ensure normalized outputs remain posix-form and deduplicated; update CLI/config path handling to surface useful errors.</developer>
<qe>
- Add tests covering invalid paths, traversal attempts, empty entries, and expected normalization results in config/CLI/discovery contexts.</qe>
<reviewer>
- Confirm new validation doesn’t break legitimate glob patterns and keeps deterministic ordering.</reviewer>
<tests>
- Commands: `ruff check . && pytest -q tests/test_config_manager.py tests/test_discovery.py`.</tests>
</task>
