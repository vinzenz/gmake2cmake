<task id="TASK-0029">
<summary>Support complex Make functions (eval, call, foreach).</summary>
<scope>
- Implement handlers for $(eval), $(call), $(foreach), $(if), $(filter).
- Add function registry in MakeEvaluator with extensible handlers.
- Create UnknownConstruct entries for unsupportable functions.
- Provide basic support where CMake equivalents exist.
- Document function support matrix in user guide.</scope>
<developer>
- Create make_functions.py with function handler registry.
- Implement foreach as CMake foreach where possible.
- Map if/filter to CMake conditionals when simple.
- Generate UnknownConstruct for eval/call with suggested manual review.
- Add function complexity analyzer for diagnostic hints.</developer>
<qe>
- Test each function with simple and complex usage.
- Verify CMake equivalents produce same behavior.
- Test UnknownConstruct generation for unsupported cases.
- Validate nested function calls handled correctly.</qe>
<reviewer>
- Check function implementations match Make semantics.
- Verify UnknownConstruct messages guide user appropriately.
- Ensure partial support doesn't mislead users.
- Validate function registry extensibility.</reviewer>
<tests>
- Commands: `pytest -q tests/evaluator/test_make_functions.py`.</tests>
</task>