<task id="TASK-0035">
<summary>Add missing type hints in cli helpers.</summary>
<scope>
- Ensure CLI helper functions (e.g., `_stdout`) have explicit return and argument type hints.
- Import required typing aliases (e.g., `TextIO`) and keep signatures consistent with usage.</scope>
<developer>
- Annotate `_stdout` and any other untyped helpers; avoid altering runtime behavior.
- Keep argparse signatures and dataclasses unchanged aside from types; tidy imports per ruff.</developer>
<qe>
- Update or add tests covering annotated helpers if behavior changes; otherwise run lint to enforce type/import correctness.</qe>
<reviewer>
- Verify annotations match actual return types and don’t introduce circular imports.</reviewer>
<tests>
- Commands: `ruff check gmake2cmake/cli.py && pytest -q tests/test_cli.py`.</tests>
</task>
