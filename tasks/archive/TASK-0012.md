<task id="TASK-0012">
<summary>Clean up CLI imports and report serialization.</summary>
<scope>
- Consolidate duplicate imports in `gmake2cmake/cli.py` (UnknownConstruct/to_dict).
- Refactor `_write_report` to reuse a shared serialization helper (diagnostics to JSON) to avoid duplicated payload construction.
- Ensure console/JSON/Markdown outputs stay consistent.</scope>
<developer>
- Merge redundant imports into a single line; run ruff for style.
- Introduce/lean on a serialization helper for diagnostics/unknowns instead of manual dict crafting in `_write_report`.</developer>
<qe>
- Add/adjust tests covering report JSON/Markdown shape post-refactor; lint pass.</qe>
<reviewer>
- Check import cleanliness and serialization consistency with existing reporter behavior.</reviewer>
<tests>
- Commands: `ruff check . && pytest -q tests/test_cli.py`.</tests>
</task>
