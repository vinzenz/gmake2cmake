<task id="TASK-0025">
<summary>Create centralized diagnostic code registry.</summary>
<scope>
- Define DiagnosticCode enum in `gmake2cmake.diagnostics.codes` with all codes.
- Ensure unique codes across all components with clear naming convention.
- Add severity level and default message templates to each code.
- Migrate all hardcoded diagnostic codes to use registry.
- Generate documentation of all diagnostic codes automatically.</scope>
<developer>
- Create enum with codes like DISCOVERY_ENTRY_MISSING, PARSER_CONDITIONAL, etc.
- Add metadata class associating codes with severity and message templates.
- Update all components to import and use registered codes.
- Add validation preventing duplicate code registration.
- Create diagnostic code documentation generator.</developer>
<qe>
- Test all diagnostic sites use registered codes.
- Verify no duplicate codes in registry.
- Test message template parameter substitution.
- Validate generated documentation completeness.</qe>
<reviewer>
- Check naming convention consistency across codes.
- Verify all existing diagnostic codes migrated.
- Ensure registry doesn't complicate diagnostic creation.
- Validate documentation is user-friendly.</reviewer>
<tests>
- Commands: `pytest -q tests/test_diagnostic_registry.py && python -m gmake2cmake.diagnostics.codes --validate`.</tests>
</task>