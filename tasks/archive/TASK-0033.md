<task id="TASK-0033">
<summary>Implement comprehensive config schema validation.</summary>
<scope>
- Add JSON Schema definition for YAML config format.
- Implement strict validation with helpful error messages.
- Support schema versioning and migration.
- Add config validation CLI command.
- Generate config template and documentation from schema.</scope>
<developer>
- Create config_schema.json with full YAML structure definition.
- Use jsonschema library for validation with custom validators.
- Add version field to config with migration support.
- Implement --validate-config CLI flag for checking configs.
- Generate template YAML with comments from schema.</developer>
<qe>
- Test validation catches all invalid configs.
- Verify error messages point to specific issues.
- Test schema migration from older versions.
- Validate generated template is valid config.
- Test with complex real-world configs.</qe>
<reviewer>
- Check schema covers all config options.
- Verify error messages are user-friendly.
- Ensure backward compatibility handling.
- Validate schema documentation completeness.</reviewer>
<tests>
- Commands: `pytest -q tests/config/test_schema_validation.py && gmake2cmake --validate-config sample.yaml`.</tests>
</task>