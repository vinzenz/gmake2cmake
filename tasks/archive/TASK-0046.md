<task id="TASK-0046">
<summary>Implement a consistent logging strategy.</summary>
<scope>
- Add structured logging support (verbosity-driven) across the pipeline in addition to diagnostics.
- Provide centralized logging configuration and CLI flags to control verbosity/output.</scope>
<developer>
- Introduce a logging setup helper (e.g., `logging_config.py`) and wire CLI flags (debug/log-file) to configure handlers/levels.
- Add lightweight log statements at key stages (discover/parse/evaluate/build/emit) without spamming output.</developer>
<qe>
- Add tests asserting logging setup honors verbosity and does not clash with diagnostics; consider capturing logs in CLI tests.</qe>
<reviewer>
- Ensure logging defaults stay quiet for normal runs and do not regress performance.</reviewer>
<tests>
- Commands: `ruff check . && pytest -q tests/test_cli.py`.</tests>
</task>
