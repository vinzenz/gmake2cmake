<task id="TASK-0041">
<summary>Centralize default values and constants.</summary>
<scope>
- Extract hard-coded defaults (paths, report filenames, config defaults, unknown-construct defaults) into a shared constants module.
- Replace scattered literals in CLI/config/unknowns with the centralized constants.</scope>
<developer>
- Create `gmake2cmake/constants.py` (or similar) containing defaults for source/output dirs, report filenames, global config files, severity strings, unknown construct defaults.
- Update consumers (`cli.py`, `config.py`, `ir/unknowns.py`, others) to import constants; keep backward-compatible behavior.</developer>
<qe>
- Add tests ensuring constants are used (e.g., report paths, default config files) and that behavior remains unchanged after refactor.</qe>
<reviewer>
- Check for deterministic defaults and that no hard-coded strings remain in core paths.</reviewer>
<tests>
- Commands: `ruff check . && pytest -q tests/test_cli.py tests/test_config_manager.py tests/test_unknowns.py`.</tests>
</task>
