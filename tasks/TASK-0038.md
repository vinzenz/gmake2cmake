<task id="TASK-0038">
<summary>Fix redundant alias assignment logic in IR builder.</summary>
<scope>
- Correct `build_targets` alias resolution for external targets so alias handling reflects intended behavior.
- Remove unreachable/contradictory conditions that always nullify alias values.</scope>
<developer>
- Simplify `final_alias` computation around `classification == "external"` vs override presence; align with override semantics.
- Add clarifying comments/tests if intent differs for imported vs external aliases.</developer>
<qe>
- Add regression test covering external target alias assignment (with and without overrides) in `tests/ir/test_ir_builder.py`.</qe>
<reviewer>
- Ensure deterministic alias selection and no impact on internal target aliasing.</reviewer>
<tests>
- Commands: `ruff check gmake2cmake/ir/builder.py && pytest -q tests/ir/test_ir_builder.py`.</tests>
</task>
