<task id="TASK-0007">
<summary>Add integration fixtures for TS21–TS25 and golden CMake outputs.</summary>
<scope>
- Create integration fixtures representing scenarios: project-global config centralization (TS21), alias linking internal vs external (TS22), INTERFACE/IMPORTED targets (TS23), packaging outputs (TS24), global vs per-target flags separation (TS25).
- Add golden outputs for emitted CMakeLists/global module/Config files; wire into e2e tests.</scope>
<developer>
- Build fixtures under tests/e2e/fixtures with Makefiles/configs; craft expected CMake outputs (golden text files) for each scenario.
- Extend CLI e2e tests to run conversions (dry-run or temp output dir) and compare generated content to goldens.</developer>
<qe>
- Validate that diagnostics are correct (WARN/ERROR where expected), aliases used for internal links, globals centralized, packaging files generated with namespace consistency.
- Ensure deterministic outputs across runs; cover dry-run vs write paths.</qe>
<reviewer>
- Confirm fixtures mirror architecture intents; golden files accurate and minimal; tests resilient (normalize paths where needed).</reviewer>
<tests>
- Commands: `ruff check . && pytest -q tests/e2e`.
- Scenarios: TS21, TS22, TS23, TS24, TS25.</tests>
</task>
