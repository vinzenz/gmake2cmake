<task id="TASK-0018">
<summary>Normalize global config with flag mapping and ignores.</summary>
<scope>
- Apply `ConfigModel.flag_mappings` and deduplication when building `ProjectGlobalConfig` (flags/defines/includes) instead of copying raw values.
- Respect `ConfigModel.ignore_paths` while capturing global sources/includes/defines/flags so ignored entries do not reach the emitter.
- Ensure the global module renders normalized content with deterministic ordering.</scope>
<developer>
- Enhance `build_project_global_config` to normalize paths to posix, apply mappings, drop ignored entries, and surface unmapped flag diagnostics when relevant.
- Align global module rendering/tests with normalized output persisted in IR.</developer>
<qe>
- Extend `tests/ir/test_ir_builder.py` and `tests/emitter/test_emitter.py` to assert mapping/dedup and ignore handling for project global configuration.</qe>
<reviewer>
- Check feature toggles/options still flow through unchanged; verify deterministic ordering and diagnostics semantics.</reviewer>
<tests>
- Commands: `pytest -q tests/ir/test_ir_builder.py tests/emitter/test_emitter.py`.</tests>
</task>
