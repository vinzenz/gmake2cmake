<task id="TASK-0072">
<summary>Add CLI/config switch for make introspection mode</summary>
<scope>
- Components: CLIController, ConfigManager, diagnostics
- Deliverables: new flag `--use-make-introspection`, config plumbed through pipeline, hard gate to keep feature opt-in
</scope>
<developer>
- Add CLI flag and config property for make introspection (default off); reject incompatible flags if any.
- Detect `make` availability in PATH when the flag is enabled; emit a clear diagnostic if missing.
- Thread the flag through RunContext/config and prepare stub options for the introspection runner (timeouts, max output size).
- Update help text, README, and schema if applicable.
</developer>
<qe>
- Add CLI parsing tests covering default/off/on and missing `make` binary path detection.
- Validate config serialization/deserialization includes the new flag and preserves defaults.
- Exercise dry-run pipeline with introspection disabled to ensure no behavior change.
</qe>
<reviewer>
- Check flag naming/defaults match request and remain backward compatible.
- Ensure diagnostics for missing `make` are actionable and deterministic.
- Confirm documentation/help text fully describe the opt-in behavior.
</reviewer>
<tests>
- pytest tests/test_cli.py -k introspection
- pytest tests/test_config_manager.py -k introspection
- Manual: gmake2cmake --use-make-introspection (with/without make in PATH)
</tests>
</task>
