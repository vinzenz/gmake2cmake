<task id="TASK-0005">
<summary>Enhance CMakeEmitter: alias-aware linking, interface/imported targets, packaging outputs.</summary>
<scope>
- Generate target_link_libraries with alias targets for internals; handle link options, include/define scopes.
- Emit INTERFACE targets for shared flag bundles; emit IMPORTED targets per overrides.
- Flesh out packaging: install/export rules, Config/ConfigVersion with namespace; stabilize global module emission.</scope>
<developer>
- Update render_target to apply PUBLIC/PRIVATE/INTERFACE usage for includes/defines/options/link libs; emit alias creation once.
- Add interface/imported target rendering paths; avoid include_directories when INTERFACE targets suffice for globals.
- Implement render_packaging with install(TARGETS ... EXPORT ...), exports with namespace, Config/ConfigVersion content.</developer>
<qe>
- Add golden tests under tests/emitter for alias linking, interface/imported targets, packaging outputs (Config, version, exports), global module behavior without duplication.
- Include negative tests: EMIT_UNKNOWN_TYPE, EMIT_WRITE_FAIL simulation, packaging namespace consistency.</qe>
<reviewer>
- Check alias/link correctness, usage scopes, deterministic ordering of generated files, and packaging completeness.
- Ensure dry-run skips writes; diagnostics surfaced for errors.</reviewer>
<tests>
- Commands: `ruff check . && pytest -q tests/emitter`.
- Scenarios: TS22 (aliases), TS23 (INTERFACE/IMPORTED), TS24 (packaging mode).</tests>
</task>
